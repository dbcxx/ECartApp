<ul class="nav navbar-nav navbar-right">
    <li>
        @if (Session["cartCounter"] == null)
        {
            @Html.ActionLink("Cart", "CartView", "Shopping", new { id = "cartItem" })
        }
        else
        {
             @Html.ActionLink("Cart(" + Session["cartCounter"]  +  ")", "CartView", "Shopping", new { id = "cartItem" })
        }
    </li>
</ul>
